server:
  port: 5010

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/msp_airline_pricing_db
    username: root
    password: Pkul@4567
  jpa:
    hibernate:
      ddl-auto: update

  config:
    import: optional:configserver:http://localhost:8888

  kafka:
    bootstrap-servers: localhost:9092

#  cloud:
#    openfeign:
#      circuitbreaker:
#        enabled: true
#        group:
#          enabled: true

razorpay:
  api:
    key: rzp_test_TM3zfbvIlBJ7II
    secret: FSv6ZhYN8In9p6gF9MzTRCwH
  callback:
    base-url: http://localhost:5173

stripe:
  api:
    key: your stripe api key

#resilience4j:
#  circuitbreaker:
#    configs:
#      default:
#        sliding-window-type: COUNT_BASED
#        sliding-window-size: 20
#        minimum-number-of-calls: 10
#        failure-rate-threshold: 50
#        slow-call-rate-threshold: 80
#        slow-call-duration-threshold: 3000ms
#        wait-duration-in-open-state: 30s
#        permitted-number-of-calls-in-half-open-state: 5
#        automatic-transition-from-open-to-half-open-enabled: true
#        register-health-indicator: true
#        record-exceptions:
#          - java.io.IOException
#          - java.util.concurrent.TimeoutException
#          - feign.FeignException
#        ignore-exceptions:
#          - com.zosh.common_lib.exception.UserException
#          - com.zosh.common_lib.exception.AirportException
#    instances:
#      UserClient:
#        base-config: default
#        failure-rate-threshold: 40

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true

info:
  app:
    name: ${spring.application.name}
    description: Handles payment processing via Razorpay and Stripe gateways
    version: 1.0.0
    environment: development

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health, info, circuitbreakers, circuitbreakerevents
#  endpoint:
#    health:
#      show-details: always
#    info:
#      enabled: true
#  info:
#    env:
#      enabled: true
#    java:
#      enabled: true
#    os:
#      enabled: true
#  health:
#    circuitbreakers:
#      enabled: true
