server:
  port: 5011

spring:
  application:
    name: pricing-service

  datasource:
    url: jdbc:mysql://localhost:3306/msp_airlines_pricing_db
    username: root
    password: Pkul@4567

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
